# 하브루타 톡 — 가족 질문 대화 앱

바쁜 하루에도 아이와 눈을 마주치고 대화할 수 있도록, ChatGPT(OpenAI API)가 오늘 있었던 일에서
꼬리에 꼬리를 무는 하브루타 질문을 대신 만들어주는 가족용 웹 앱입니다.

## 핵심 기능

- **채팅형 인터페이스**: 가족이 오늘 있었던 일을 입력하면, AI가 그 내용을 이어받아 다음 질문을
  하나씩 만들어줍니다. 화면 상단에는 "지금 이 질문"이 큰 카드로 뜨는데, 이건 채팅을 보라는 게
  아니라 **그 질문을 함께 읽고 눈을 마주치며 이야기 나눈 뒤, 그 대화 내용을 채팅창에 기록**하라는
  용도입니다.
- **가족 연동**: 가족 코드(6자리)로 여러 기기에서 같은 가족 대화방에 참여합니다. 부모/아이 여러 명이
  같은 하루 세션에 이야기를 남길 수 있습니다.
- **나이대별 질문 층위**: 태어난 연도로 나이대(유아/어린이/초등고학년/청소년/성인)를 계산하고,
  나이대에 맞는 질문 깊이 범위 안에서 AI가 질문을 만듭니다.
- **질문 층위 점수화**: 하브루타 질문을 5단계 사다리로 분류합니다.
  1. 사실 확인 → 2. 감정 탐색 → 3. 이유 탐구 → 4. 상상·가정 → 5. 가치·적용
  대화가 이어질수록 한 단계씩 깊어지도록 유도하고, 오늘의 평균 깊이 / 누적 질문 수 /
  연속 대화 일수를 통계로 보여줍니다.
- **오늘 얘깃거리가 없을 때**: "오늘 얘깃거리가 없어요" 버튼을 누르면 AI가 무작위 그림 주제와
  그 그림으로 시작하는 첫 질문을 만들어줍니다(이미지 생성 실패 시에도 질문은 정상 진행).
- **지난 기록**: 채팅 중심 UI를 유지하면서도, 지난 날짜별 대화를 목록으로 훑어보고 다시 읽을 수
  있는 가벼운 기록 페이지를 제공합니다.

## 기술 스택

- Next.js 14 (App Router) + TypeScript + Tailwind CSS
- Prisma + PostgreSQL (가족/구성원/일별 세션/메시지) — Vercel 같은 서버리스 배포에서는
  로컬 파일(SQLite)이 재배포마다 초기화되기 때문에 Postgres를 씁니다.
- OpenAI API (`gpt-4o-mini`로 질문 생성, `gpt-image-1`로 무작위 주제 이미지 생성).
  생성된 이미지는 서버리스 환경이 디스크에 못 쓰므로 base64로 DB에 바로 저장합니다.
- 로그인은 별도 회원가입 없이 "가족 코드 + 닉네임"만으로 참여하는 경량 방식입니다
  (localStorage에 신원을 저장). 더 강한 보안이 필요하면 NextAuth 등으로 교체하세요.

## 로컬 개발

계정 가입 없이 바로 개발하려면 docker로 로컬 Postgres를 띄우세요.

```bash
npm install
cp .env.example .env
# .env 에 OPENAI_API_KEY 를 실제 키로 채워주세요 (서버에서만 사용, 브라우저에 노출되지 않음)

docker compose up -d   # 로컬 Postgres (localhost:5432, .env.example 기본값과 일치)
npm run db:push         # 스키마 반영
npm run dev              # http://localhost:3000
```

Neon/Vercel Postgres 같은 클라우드 DB를 로컬에서 바로 쓰고 싶다면, docker 없이
`.env`의 `POSTGRES_PRISMA_URL` / `POSTGRES_URL_NON_POOLING`만 그 DB 주소로 바꾸면 됩니다.

## 배포하기 (Vercel)

로또추첨기(260711)와 같은 방식으로, Vercel에 GitHub 저장소를 연결해서 배포합니다.
(이 코드를 만든 세션은 vercel.com에 대한 아웃바운드 접속이 막혀 있어서 CLI로 직접
배포는 못 하고, 아래 순서대로 대시보드에서 클릭 몇 번이면 끝나게 준비해뒀어요.)

1. **저장소 임포트**: [vercel.com/new](https://vercel.com/new) → `choiyeonah-hub/yeonah`
   저장소 선택 → Import. (Framework는 Next.js로 자동 인식됩니다.)
2. **Postgres 준비**: Vercel의 Storage 연동을 써도 되고(있다면), 아니면
   [neon.tech](https://neon.tech)에서 무료로 DB를 하나 만들어서 나오는
   **Connection string**을 그대로 써도 됩니다.
3. **환경변수 등록**: 프로젝트 **Settings → Environment Variables**에서 아래 세 개를
   추가합니다 (Production 체크). Neon처럼 연결 문자열이 하나뿐이면 두 값에 똑같이
   넣으면 됩니다.
   - `POSTGRES_PRISMA_URL`
   - `POSTGRES_URL_NON_POOLING`
   - `OPENAI_API_KEY`
4. **Deploy** 클릭. **테이블은 따로 만들 필요 없습니다** — `package.json`의
   `vercel-build` 스크립트가 빌드할 때마다 `prisma db push`를 자동으로 실행해서
   스키마를 최신 상태로 맞춰줍니다(Vercel이 이 스크립트를 자동으로 감지해서 씁니다).
   이후로는 `claude/habruuta-chatgpt-app-81uq8q` 브랜치(또는 main)에 푸시할 때마다
   자동 재배포되고, 그때마다 스키마도 같이 동기화됩니다.

> 이미지 생성 기능을 쓰려면 OpenAI 계정에 `gpt-image-1` 사용 권한이 있어야 합니다.
> 권한이 없어도 앱은 죽지 않고, 그림 없이 질문만 정상적으로 생성됩니다.
>
> `vercel-build`는 `prisma db push --accept-data-loss`를 씁니다. 이후 스키마를 바꿀 때
> 컬럼을 지우거나 타입을 바꾸는 등 파괴적인 변경이 있으면 데이터가 조용히 사라질 수 있으니,
> 실제 가족 데이터가 쌓인 뒤에 스키마를 바꿀 땐 이 부분을 참고하세요.

## 폴더 구조

```
prisma/schema.prisma       # Family / Member / DaySession / Message
src/lib/depth.ts           # 질문 층위 사다리 + 나이대 계산
src/lib/openai.ts          # 꼬리질문 생성 / 무작위 이미지 주제 생성
src/app/api/*              # family, session, chat, topic, stats API
src/app/page.tsx           # 가족 만들기 / 참여하기
src/app/chat/page.tsx      # 오늘의 하브루타 채팅
src/app/history/*          # 지난 기록 목록 / 상세
```

## 참고 사항

- 이미지 생성(`gpt-image-1`)은 계정에 따라 별도 인증이 필요할 수 있습니다. 실패해도 앱은
  질문 생성까지는 계속 진행하도록 만들어져 있습니다.
- 배포 환경에서 `OPENAI_API_KEY`가 없거나 OpenAI 도메인에 대한 아웃바운드 네트워크가 막혀 있으면,
  사용자의 답변은 정상 저장되고 질문 생성 단계에서만 안내 메시지가 표시됩니다.

---

# 아이핏 (EyeFit) — 얼굴에 맞는 안경 찾기

같은 저장소 안에 있는 두 번째 앱입니다. `/glasses` 경로로 들어갑니다.
얼굴 사진과 안경 처방전을 AI가 읽어서 **테 + 렌즈**를 함께 추천하고,
동네·전국 안경원의 예상 견적을 비교해 **방문 예약**까지 하는 흐름입니다.

## 왜 만들었나

시중 안경 사이즈 규격은 서구권 두상을 기준으로 만들어진 게 많습니다. 코받침이 낮아
흘러내리거나, 전체폭이 커서 관자놀이에 안 맞는 일이 그래서 생깁니다. 아이핏은
성별이나 인종을 전제한 "표준 두상"을 쓰지 않고, **사진에 보이는 실제 비율**로 계산합니다.
성별을 묻지 않고 외모 점수도 매기지 않으며, 누구나 같은 방식으로 씁니다.

## 다섯 단계

1. **얼굴 비율 재기** — 사진 한 장으로 얼굴형(8종)·얼굴폭·콧대 높이·눈 간격·눈썹 라인을
   측정합니다. 사진을 올리기 싫으면 얼굴형을 직접 골라도 끝까지 진행됩니다.
2. **도수 읽기** — 안과·안경원 처방전을 찍어 올리면 SPH / CYL / AXIS / ADD / PD를 읽어
   채워줍니다. 직접 입력해도 되고, 도수를 몰라도 "매장에서 검안" 으로 넘어갈 수 있습니다.
3. **테 고르기** — 아래 다섯 축을 합산해 0~100점의 적합도를 매깁니다.
   | 축 | 배점 | 내용 |
   | --- | --- | --- |
   | 얼굴형 × 테 모양 | 40 | 대비 원칙 (둥근 얼굴엔 각진 테, 각진 얼굴엔 곡선 테) |
   | 얼굴폭 × 테 전체폭 | 20 | 크면 흘러내리고 작으면 관자놀이에 자국이 남는다 |
   | PD × (렌즈폭+브릿지) | 15 | 눈동자가 렌즈 광학 중심에 오는지 |
   | 콧대 × 코받침 | 15 | 콧대가 낮으면 아시안핏 / 코패드 조절형 |
   | 도수 × 테 구조 | 10 | 고도수는 좁은 렌즈폭 풀테가 두께를 덜 드러낸다 |
   누진 처방인데 렌즈 세로폭이 34mm 미만이면 근용부가 안 들어가므로 크게 감점합니다.
4. **렌즈 고르기** — 실효 도수(|SPH| + |CYL|/2)로 굴절률(1.56 / 1.60 / 1.67 / 1.74)을
   정하고, 화면 사용 시간·야외 활동으로 블루라이트·변색 코팅을 제안합니다.
   난시 2D 이상, 부동시 2D 이상, PD 누락, 1년 넘은 처방전은 주의 문구로 짚어줍니다.
5. **가격 비교 · 예약** — 매장별 테/렌즈 할인율로 예상 견적을 계산해 싼 순으로 보여주고,
   6자리 예약 코드를 발급합니다. 금액은 클라이언트 값을 믿지 않고 서버에서 다시 계산합니다.

## 개인정보 · 법적 경계

이 앱이 다루는 정보는 대부분 민감합니다. 그래서 아래 선을 코드로 지켜뒀습니다.

- **얼굴 사진과 처방전 이미지는 저장하지 않습니다.** 요청을 처리하는 동안에만 메모리에
  있다가 응답과 함께 사라집니다 (`analyze-face`, `read-prescription` 라우트는
  `dynamic = "force-dynamic"` + `Cache-Control: no-store`).
- DB에 남는 건 얼굴형·비율 같은 **파생 숫자**뿐이고, 그것도 사용자가 매장 예약을 확정할 때만
  기록됩니다. 마법사 진행 중의 상태는 localStorage에도 넣지 않고 메모리에만 둡니다.
- 처방 도수는 개인정보보호법상 **민감정보**입니다. 매장에 전달할지는 예약 단계에서 별도
  체크박스로 동의를 받고(`sensitiveConsent`), 동의하지 않으면 아예 저장하지 않습니다.
  동의 없이도 예약은 되고, 그 경우 매장에서 안경사가 직접 검안합니다.
- 얼굴 분석 프롬프트는 외모 평가·매력도 점수·인종/성별/나이 추정을 금지하고, 안경 사이즈
  계산에 필요한 기하학적 비율만 뽑도록 제한했습니다.
- **앱에서 결제하지 않습니다.** 「의료기사 등에 관한 법률」상 도수 안경·콘택트렌즈는
  안경사만 판매할 수 있고 전자상거래로는 판매할 수 없습니다. 그래서 이 앱은 추천 →
  견적 비교 → 방문 예약까지만 하고, 최종 검안·조제·피팅·결제는 안경원에서 이뤄집니다.
  (실제 서비스로 낼 때는 이 구조를 변호사에게 한 번 확인받으세요.)
- 아이핏은 의료기기가 아니며 진단이나 처방을 하지 않습니다. OCR로 읽은 도수는 "매장에서
  확인할 초안"이지 확정 처방이 아닙니다.

## 폴더 구조

```
src/lib/glasses/types.ts       # 공유 타입 (얼굴 분석 / 처방 / 테 / 매장)
src/lib/glasses/faceShapes.ts  # 얼굴형 8종 + 궁합 테 모양 (대비 원칙)
src/lib/glasses/frames.ts      # 테 카탈로그 (모양·사이즈·코받침·무게)
src/lib/glasses/lenses.ts      # 굴절률/코팅 카탈로그 + 도수 기반 렌즈 추천
src/lib/glasses/stores.ts      # 제휴 안경원 + 매장별 견적 계산
src/lib/glasses/fit.ts         # 테 적합도 점수 엔진 (5개 축)
src/lib/glasses/vision.ts      # 얼굴 비율 측정 / 처방전 판독 (OpenAI 비전)
src/app/api/glasses/*          # analyze-face, read-prescription, quote, reservation
src/app/glasses/page.tsx       # 소개 + 개인정보 처리 방식
src/app/glasses/fit/page.tsx   # 5단계 마법사
src/app/glasses/reservation/*  # 예약 코드 확인 화면
src/components/glasses/*       # 단계별 UI + 테 모양 SVG
```

테·렌즈·안경원 데이터는 데모용 카탈로그입니다. 실제 서비스에서는 제휴 매장/유통사
카탈로그로 갈아끼우면 되고, 추천 로직이 쓰는 필드(모양·사이즈·코받침·무게·할인율)만
채워지면 나머지는 그대로 동작합니다.

## 배포

하브루타 톡과 같은 Next.js 프로젝트라 배포 방법이 같습니다. 위의 Vercel 안내를 그대로
따르고, 환경변수에 `OPENAI_VISION_MODEL`(선택)만 추가하면 됩니다. `vercel-build`가
`prisma db push`를 돌리므로 `FitReservation` 테이블도 자동으로 만들어집니다.
