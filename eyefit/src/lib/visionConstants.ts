/**
 * 사람의 홍채(각막) 가로 지름은 개인차가 작아 평균 약 11.7mm다.
 * 사진 속 길이를 mm로 바꿀 때 이 값을 자로 쓴다.
 *
 * vision.ts는 서버 전용(OpenAI SDK를 불러온다)이라, 클라이언트 컴포넌트가
 * 이 상수만 쓰려고 서버 모듈을 끌어오지 않도록 따로 뺐다.
 */
export const IRIS_WIDTH_MM = 11.7;
