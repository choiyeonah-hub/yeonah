# Postgres 접속 정보.
# - Vercel: 프로젝트의 Storage 탭에서 Postgres를 연결하면 아래 두 값이 자동으로 채워집니다.
# - 로컬 개발: docker-compose.yml로 띄운 Postgres를 그대로 써도 됩니다(README 참고).
POSTGRES_PRISMA_URL="postgresql://habruta:habruta@localhost:5432/habruta?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgresql://habruta:habruta@localhost:5432/habruta"

# OpenAI API key (server-side only, never exposed to the browser)
# Used for: 하브루타 꼬리질문 생성, 나이대별 깊이 조절, 무작위 이미지 주제 생성
OPENAI_API_KEY="sk-..."

# Model overrides (optional)
OPENAI_CHAT_MODEL="gpt-4o-mini"
OPENAI_IMAGE_MODEL="gpt-image-1"

# 스타일 진단(/style)에서 사진 분석에 쓰는 비전 모델. 없으면 OPENAI_CHAT_MODEL을 그대로 씁니다.
# 사진 없이(문진 + 직접 측정) 쓰면 키 없이도 진단이 동작합니다.
OPENAI_VISION_MODEL="gpt-4o-mini"

# ── 쇼핑 추천 (/style/shop) ────────────────────────────────────────────────
# 쿠팡 파트너스 Open API 키. 없으면 검색어와 쇼핑몰 검색 링크만 제공합니다(그 부분은 항상 동작).
# 검색 API는 시간당 10회 제한이라 결과를 캐시해서 씁니다.
COUPANG_ACCESS_KEY=""
COUPANG_SECRET_KEY=""
# COUPANG_SEARCH_HOURLY_LIMIT="10"
# SHOP_CACHE_TTL_MS="21600000"
# 쿠팡이 경로를 바꾸면 아래 두 값으로 덮어쓸 수 있습니다.
# COUPANG_SEARCH_PATH="/v2/providers/affiliate_open_api/apis/openapi/products/search"
# COUPANG_DEEPLINK_PATH="/v2/providers/affiliate_open_api/apis/openapi/v1/deeplink"

# ── 가상 피팅 (/style/tryon) ──────────────────────────────────────────────
# FASHN.ai API 키. 이미지 1장당 과금되므로 사용자가 버튼을 누를 때만 호출합니다.
FASHN_API_KEY=""
# FASHN_MODEL="tryon-v1.6"
